spring:
  application:
    name: chat-service-microsservice
openai:
  api:
    key: ${OPENAI_API_KEY}
    url: ${OPENAI_API_URL:https://api.openai.com/v1/responses}
    model: ${OPENAI_MODEL:gpt-4.1-mini}
